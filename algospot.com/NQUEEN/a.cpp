// Copyright (C) 2017 by iamslash
// https://algospot.com/judge/problem/read/NQUEENS

#include <cstdio>
#include <vector>
#include <string>
#include <cstring>

int N;
int Q[12];

bool Nqueen(int y, int x) {
  
}

int Solve() {
  int r = 0;

  for (int y = 0; y < N; ++y) {
    
  }
  
  return r;
}

int main() {
  int T;
  scanf("%d", &T);
  for (int t = 0; t < T; ++t) {
    memset(Q, -1, sizeof(Q));
    scanf("%d", &N);
    printf("%d\n", Solve());
  }
}
